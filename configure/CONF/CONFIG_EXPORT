# Variables should be transferred to module_name.makefile


EXPORT_VARS+=SUDO


EXPORT_VARS+=$(filter %_VERSION,$(.VARIABLES))
export_version=$(eval export $1)
$(eval $(foreach v,$(EXPORT_VARS),$(call export_version,$v)))

VARS_EXCLUDES+=export_version

